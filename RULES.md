# 规则列表与用法

## 用法

插件安装后自动生效，无需配置。AstrBot 每次回复发送前自动扫描所有纯文本段，命中规则即打码。

效果示例：

| 原文 | 打码后 |
|---|---|
| 手机号 13800138000 | `138********` |
| 密码:abc123xyz | `密码:***` |
| api_key=sk-abcdef... | `api_key=***` |
| 公网IP 8.8.8.8:25565 | `***.***.***.***` |

## 规则列表

### 1. Token / Key
- `sk-` 开头（OpenAI / DeepSeek 风格），后接 ≥10 位字母数字
- `gh[opu]_` 开头（GitHub PAT），后接 ≥10 位字母数字
- 连续 32 / 40 / 64 位十六进制（frp token、MD5 / SHA 型密钥）

### 2. 键值对（key=value / key: value）
- key 词：`api_key` / `access_key` / `secret` / `token` / `password` / `passwd` / `pwd` / `密码` / `口令` / `密钥` / `key`
- 分隔符：半角/全角冒号、空格、反引号、引号（兼容 markdown 代码包裹）
- 值：≥6 位字母数字，**首字符排除 `/`**（避免把 "手机号/密码/token" 这类列举误判为键值对）
- 命中后整段值打码为 `***`

### 3. 手机号
- 大陆手机号：`1[3-9]` 开头、共 11 位连续数字
- 只保留前 3 位，后 8 位全部打码（防暴力枚举）
- 带空格/连字符的号码不触发（要求连续数字）

### 4. IP 地址
- 公网 IPv4（含可选 `:端口`）→ 整段打码为 `***.***.***.***`
- 内网/保留网段放行：192.168.*、10.*、172.16-31.*、127.*、169.254.*、组播/保留段

## 放行清单

- 内网 IP（见上）
- 公网域名（如 `ysnsm.top`）不打码

## 变更日志

- v1.0.2：修复 `_KV_RE` 误报——值首字符排除 `/`，避免 "手机号/密码/token" 枚举被误判为键值对
- v1.0.1：修复 markdown 反引号包裹 token 时正则漏过的问题；新增独立 32/40/64 位 hex 打码规则
- v1.0.0：初始版本，IP/端口/键值对打码
